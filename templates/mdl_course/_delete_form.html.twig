<form method="post" action="{{ path('mdl_course_delete', {'id': mdl_course.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mdl_course.id) }}">
    <button class="btn">Delete</button>
</form>
