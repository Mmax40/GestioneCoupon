<form method="post" action="{{ path('costo_coupon_crud_delete', {'id': costocoupon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ costocoupon.id) }}">
    <button class="btn">Delete</button>
</form>
